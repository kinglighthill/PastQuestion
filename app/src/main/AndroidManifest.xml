<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kca.www.pastquestion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GPCalc"
            android:label="@string/title_activity_gpcalc"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CBTGame"
            android:label="@string/title_activity_cbtgme" />
        <activity
            android:name=".GameActivity"
            android:label="@string/title_activity_cbtgme" />
        <activity
            android:name=".GPCalcSettingsActivity"
            android:label="@string/title_activity_gpcalc_settings" />
        <activity android:name=".PinSalesActivity" />
        <activity android:name=".AboutUsActivity"></activity>
    </application>

</manifest>